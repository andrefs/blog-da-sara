<!DOCTYPE html>
{{ debug }}
<html>
    {{> head data.fields site=site }}
    <body>
        {{> header}}
        <div class="container">
            <div class="row">
                {{> left-sidebar}}
                {{> article/content data.fields}}
                {{> right-sidebar}}
            </div>
        </div>
        {{> footer}}
        {{> footer-scripts}}
    </body>
</html>

